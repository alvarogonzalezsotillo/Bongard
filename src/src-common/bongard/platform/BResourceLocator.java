package bongard.platform;

import bongard.util.BImplementations;
import bongard.util.IBImplementations;

/**
 * Stores information about how to locate a Bongard test (file, resource ID, ...) 
 * @author alvaro
 */
public class BResourceLocator extends BImplementations implements IBImplementations{
	public BResourceLocator(Object obj) {
		super(obj);
	}
}
