package purethought.platform;

import purethought.util.BImplementations;
import purethought.util.IBImplementations;

/**
 * Stores information about how to locate a Bongard test (file, resource ID, ...) 
 * @author alvaro
 */
public class BImageLocator extends BImplementations implements IBImplementations{
	public BImageLocator(Object obj) {
		super(obj);
	}
}
