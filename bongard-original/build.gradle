apply plugin: 'java'

dependencies {
    compile( project(':bongard-common') ){
        transitive = false
    }
    compile( project(':ollitos-common') ){
        transitive = false
    }

}

sourceSets.main.resources.srcDir 'src/main/java'