apply plugin: 'java'

dependencies {
    compile project(':bongard-original')
    runtime project(':bongard-original')

    runtime project(':bongard-quiz')
    compile project(':bongard-quiz')
}

sourceSets.main.resources.srcDir 'src/main/java'